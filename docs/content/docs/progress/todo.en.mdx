---
title: TODO
description: Follow-ups worth handling after the current release
---

# TODO

This page tracks follow-ups that are still worth doing for the project.

- GitHub release deployment acceptance: structured deployment contracts for the seven Compose profiles and the Render Blueprint are already in the release gate. Render declares paid Web/Worker, a shared maintenance token, private networking, PostgreSQL, and a 10GB disk. Local release checks already verify the custom `distDir` standalone server, non-empty static assets, and complete public branding assets. The production Dockerfile also loads Sharp during amd64/arm64 image builds and blocks images missing libvips. Next, on a clean Linux server still run `docker compose config`, image pull, PostgreSQL health checks, `/install` initialization, generation operations summary and media stats, Worker heartbeats, image WebP previews, per-asset static requests, HTTPS reverse proxy, and volume restart recovery; on a real Render account still verify Blueprint resources, cost, first deploy, Worker heartbeats, and disk restore.
- Short-drama pipeline follow-ups: rebuild the production Docker image and accept two-phase script analysis, three video modes, shot transcoding, voiceover mix, CJK subtitle burn-in, Jianying 5.x/6+ draft import, task cancel, and final download with real text/image/video/audio channels. Continue full-episode history, long-term cost archives, and media access load tests.
- Upstream availability acceptance: local protocol fixtures already cover text/image/video/audio, async polling, and failed refunds; four real TCP responses also pass through app runtime/adapters. Temporary channels, logical models, and defaults were deleted after acceptance. Production-candidate default models are still empty—fixture success is not vendor availability. After adding real keys or manual models for Agnes and other vendors, save catalog pagination, minimal calls, real media for image/video/audio, polling, retries, failed refunds, and ledger evidence before setting production defaults.
- Creative Agent follow-ups: add trustworthy cover/keyframe extraction and multi-frame review for video outputs; extend visual similarity checks for subjects, brand elements, and references on top of existing real-image checks; continue long-term points archives, shot version diffs, episode history, and cost/quality analysis.
- Unified generation executor follow-ups: page GET/SSE can already wake the same durable task and recover upstream results; ordinary Agent review is already a durable Worker task; PostgreSQL Run events have `LISTEN/NOTIFY` plus polling fallback. Next configure maintenance tokens and monitor `generation-worker` in default Docker, Baota, external-DB, and at least two app instances so query/media/review still finish after the user closes the page; record notify loss, restart, and lease takeover evidence. Continue real upstream cancel, duplicate-create protection, media reference stats, and delete races.
- Reference media deployment acceptance: use an externally reachable `NEXT_PUBLIC_SITE_URL` in production so upstream can read in-site images/video/audio; verify generation consistency, history restore, and temp cleanup. Dev loopback must not silently fall back to reference-free generation.
- Third-party relay follow-ups: on top of logical-model bindings, add channel circuit breakers, weighted routing, webhook callbacks, and authenticated media re-store; maintain protocol acceptance samples per live platform.
- Protocol platform follow-ups: promote channel-level custom protocol drafts into immutable AdapterRevisions; complete OpenAPI/Postman/HAR structured parse, field provenance and version diffs, static validation, publish, canary, and rollback. Complex cloud signatures, WebSocket, ComfyUI workflows, and proprietary upload sessions must use reviewed native Drivers—never execute admin scripts.
- Security and concurrency follow-ups: keep accepting auth writes, refund claims, event replay, and failed refunds under multi-instance PostgreSQL, real reverse proxies, and payment-provider callback load. On media, use large local videos and private S3 to verify user/signature/resource/IP layered rate limits, GET/HEAD, 32 MiB Range segments, scrubbing, concurrent cancel, file handles, pool waits, egress, and hot-resource 429 false positives.
- Site-wide performance follow-ups: first-screen data, request counts, pagination/cache, SEO, and known-risk matrices exist for 31 pages; `/assets` is server-paginated and Canvas splits list summary from project detail. Local PostgreSQL already recorded `EXPLAIN (ANALYZE, BUFFERS)` and index paths for finance, generation overview, create home, Canvas, assets, drama cost, and conversation pages. Still add production-scale per-page response bytes, P50/P95/P99, pool/lock waits, slow queries, and formal build/bundle evidence.
- Async generation commercial acceptance: with real image/video/audio/Agent channels verify queueing, timeout, cancel, retry, disconnect recovery, duplicate submit, stuck-task takeover, failed refunds, and balance/ledger/media registration checks. Define max retries, manual ledger repair, upstream pause, and degradation rules.
- Engineering quality follow-ups: introduce ESLint rules compatible with Next.js 16 / React 19 and an in-repo browser E2E baseline; gradually raise Route Handler contract coverage without rewriting every route at once.
- Commercial launch verification: local compatible signed callbacks already cover success, duplicate, concurrent claim, invalid signature, and failure release. Still complete per-provider sandbox/pre-production order, callback, duplicate/out-of-order/late callback, payment timeout, full refund, and reconciliation. Official Alipay and face-to-face must be verified separately; face-to-face needs precreate QR, scan, QR expiry, and async notify evidence. Use real PostgreSQL to stress concurrent coupon claims, concurrent orders for the same coupon, cancel/timeout release, late payment conflicts, and post-refund non-return of coupons; provider settlement must equal the order snapshot. Auto refund is whole-order today—if partial refunds are required, add order/payment/points/accounting rules separately. Legal counsel must review terms, privacy, refund, IP, and content liability for the real operator, regions, data location, and refund rules, and confirm registration consent capture, agreement version, and consent timestamps; current pages are product baseline text only.
- Invite growth production acceptance: with real PostgreSQL, at least three accounts, and real/sandbox payments verify invite visit, atomic registration binding, first-order threshold, cool-down maintenance, duplicate payment callbacks, concurrent settle, monthly/campaign caps, same-network/payment-identity risk signals, refund reverse point ledgers, and manual review of locked/redeemed coupons. Before launch define risk thresholds, freeze notices, appeals, false-positive recovery, and ops on-call.
- Production observability and capacity: choose optional error tracking, structured logs, host/container metrics, and alerts per deploy profile; add pool utilization, slow queries, API P95/P99, payment success rate, and generation success rate. Finish 100/500/1000 concurrency tiered load tests before introducing Redis, a separate queue, or larger pools—do not mechanically grow infra without evidence.
- Commercial ops follow-ups: server media backup and capacity alerts; full DB/media/OSS restore drills; real provider bill sample libraries; automated bill fetch/periodic reconcile; invoice/tax policy; support escalation; refund review and basic abnormal-payment risk controls.
- Commercial security and service governance: admin MFA/abnormal-login protection and high-risk action review; rotation/revocation/leak response for model keys, payment certs, maintenance tokens, and OSS credentials; chargeback/dispute handling, data lifecycle, S3 cleanup, storage/bandwidth cost alerts, status page/outage notices, and accessibility acceptance evidence.
- Security incidents and vendor exit: maintain a severity playbook for data leak, key leak, privilege abuse, private media exposure, and payment data anomalies; drill isolation, forensics, notice, session revoke, key rotation, backup restore, and postmortems. Keep migration/export/alternatives for model, payment, SMTP, OSS, and notification vendors under stoppage/ban/price/cross-border change.
- Commercial launch controls: verify real delivery of registration, verification codes, password reset, payment results, and outage notices; complete SPF/DKIM/DMARC, bounce/complaint handling, and fallback notices; isolate dev/staging/prod DB, object storage, payment merchants, model keys, and callback domains; drill pre-migration backup, smoke, rollback, and post-rollback data compatibility; add per-user/task/day/site cost caps and abnormal circuit breakers.
- User rights and content compliance: on top of deletion requests, work reports/appeals, manual disposition, and audit logs, define formal deletion execution, creative-data deletion, statutory order/payment retention, media reference cleanup, identity review, and response SLAs; establish infringement intake, human on-call, disposition SLAs, false-positive review, and live drills. Short-drama user content confirmation gates do not replace platform compliance review.
- Works community ops acceptance: with likes/unlike, author follow, public creator pages, self following/followers/likes modals, unfollow/unlike/block, interaction notifications, and 7/30-day rankings already in the public UI, use at least two real accounts and public works to accept follow/unfollow, mutual block/unblock, list refresh, follower relations, profile modals, like history, user/admin take-down, permanent delete after take-down, source media retention, ban linkage, and ranking stability; complete 390px/430px device layout checks. Before launch still add anti-abuse monitoring, content disposition, privacy, review, and on-call rules.
- Vendor and security governance: maintain contracts/quotas/SLA/data-processing inventories for model, payment, SMTP, object storage, and notification services; clarify stoppage alternatives, user content ownership, AI-generated labeling, age limits, and acceptable use. Dependency audits are in the release gate; still finish AGPL/third-party license, container image and secret-leak scans, and vulnerability disclosure process.
- Growth and experience follow-ups: define revenue, growth, retention, paid conversion, ARPU, and repurchase metrics; build first-run guidance, first successful work path, plan upgrade/renewal strategy, and necessary batch ops. Prefer real task-stage progress over fake remaining-time estimates from upstream.
