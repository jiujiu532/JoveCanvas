/**
 * TypeScript entry kept for IDE navigation; runtime CLI is import-prompt-seeds.mjs (jiti).
 * Prefer: pnpm run import:prompt-seeds -- --source youmind-skill --input <path> --dry-run
 */
console.error("Use: pnpm run import:prompt-seeds -- --source youmind-skill|gptimage2-json --input <path> [--dry-run|--apply]");
process.exitCode = 1;
